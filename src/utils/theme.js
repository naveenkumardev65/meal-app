import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#2f2f2f',
        },
        secondary: {
            main: '#edf2ff',
        },
        buttonColor: {
            main: '#474847',
        },
        buttonColor1: {
            main: '#938c8c'
        }
    },
});

export default theme;